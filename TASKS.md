# Outstanding work

Porting from the old sandbox is finished. This is the only open-work list.

## Training

- Progressive limits: after a clear, auto-raise course difficulty (taller tower, wider gap, rougher terrain) for climb and rough-terrain goals.
- Optional extras: **Copy my demo** (seed a pack from a short manual/sine recording) and **Mix goals** (temporary reward blend on the active goal). Race-your-record and messy bodies already ship.



## World / bodies (optional deepen)

- Climb: beam / parkour variants beyond the static step course.
- Rigid struts: plate-style colliders (struts are edge-only fixed joints today).

When a slice ships, delete it from this file. New Rapier body types, joints, materials, or force models still need an ADR (`docs/CAPABILITY_ADR_TEMPLATE.md`) and `npm run smoke:all`.