# Vercel Blob — for public creature sharing.
# On Vercel, connecting a Blob store usually sets BLOB_STORE_ID (OIDC).
# BLOB_READ_WRITE_TOKEN is optional fallback (local / non-Vercel).
BLOB_STORE_ID=
BLOB_READ_WRITE_TOKEN=
