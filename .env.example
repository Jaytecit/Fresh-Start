# Vercel Blob — required for public creature sharing in production.
# Create a Blob store in the Vercel project, then copy the read/write token here
# (and into Vercel Project Settings → Environment Variables).
BLOB_READ_WRITE_TOKEN=
